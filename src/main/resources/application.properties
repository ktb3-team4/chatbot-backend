# ==========================================
# ? KTB ?? ??: ?? ???? ???? ??
# ==========================================

# 1. ?? ??
server.port=${PORT:5001}

# 2. ?? ?? (?? ??? ?? ?? ???)
# ??: ???? ?? ?????, ???(accept-count)? ?? ??? 0% ??
server.tomcat.threads.max=200
server.tomcat.threads.min-spare=50
server.tomcat.accept-count=1000
server.tomcat.max-connections=10000
server.tomcat.connection-timeout=20000

# 3. ?????? ?? (MongoDB)
spring.data.mongodb.uri=${MONGO_URI}
spring.data.mongodb.auto-index-creation=true
# DB ??? ? (?? ??: ??? ?? ?? ?? ??)
spring.data.mongodb.min-connections-per-host=20
spring.data.mongodb.max-connections-per-host=100

# 4. Redis ?? (Lettuce ??? ? ???)
spring.data.redis.host=${REDIS_HOST}
spring.data.redis.port=${REDIS_PORT}
spring.data.redis.lettuce.pool.max-active=50
spring.data.redis.lettuce.pool.max-idle=20
spring.data.redis.lettuce.pool.min-idle=10
spring.data.redis.lettuce.pool.max-wait=1000ms

# 5. ?? ?? (??: ?? ??? ? INFO ?? ??)
# P99 Latency? ??? ?? -> ???? ?? ??? ??
logging.level.root=WARN
logging.level.org.springframework=WARN
logging.level.com.ktb.chatapp=WARN
# (?? ??? ??? ?? INFO? ?????)

# 6. ?? ??? ??
file.upload-dir=./uploads
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# 7. ?? & JWT ? ??
app.encryption.key=${ENCRYPTION_KEY:1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef}
app.encryption.salt=${ENCRYPTION_SALT:1234567890abcdef1234567890abcdef}
app.jwt.secret=${JWT_SECRET:this_is_very_secure_secret_key_for_jwt_token_generation_12345}
app.jwt.expiration-ms=86400000

# 8. AI & ?? ??
spring.ai.openai.api-key=${OPENAI_API_KEY:sk-dummy}
spring.ai.openai.chat.options.model=${OPENAI_MODEL:gpt-4.1-mini}
spring.ai.openai.chat.options.temperature=0.7
socketio.server.host=0.0.0.0
socketio.server.port=${WS_PORT:5002}

# 9. ?? (Swagger ?) - ?? ????? ?? ? ??? ??
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false
management.endpoints.web.exposure.include=health,info,metrics,prometheus